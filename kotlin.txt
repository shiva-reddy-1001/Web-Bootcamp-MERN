Kotlin

fun main(){
println("hello")
}

val name : string = "data"
name = "...//" //error, cant be modified like const
solution use var instead of val

types are non null by default, string and nullable string are differnent
var name : string? = null/value

if(greeting!=null) prinln(greeting) 
else println("hi")

when(greeting)
{
     null -> prinln("hi") 
     else -> prinln(greeting)
}

fun getGreeting() : String {
     return  "hello"
}

fun getGreeting() = "Hello"

write Unt as return if not returning any or just dont specify any like main

val data = arrayOf(a,b,c)
properties...size,[],get,equals,clone,hash,

for (item in data) {
prinln(item)
}

data.forEach{it : String
prinln(it)
}

forEachIndexed {index,item ->
}

val  m = mapOf(1 to "a",2 to  "b"...)
m.forEach{key,val -> prinln("$key : $val")}

val m = mutableListOf(...)
m.add(...)

val m = mutableMapOf(...)
m.put(...)

parameters.... List<String> (type)  or vararg item:String
